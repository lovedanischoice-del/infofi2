# Infofi2 프로젝트 구조 분석

## 📋 프로젝트 개요

**프로젝트명**: Crypto Mission Tracker (크립토 미션 트래커)  
**기술 스택**: React 18 + TypeScript + Vite  
**스타일링**: TailwindCSS (CDN)  
**주요 기능**: 크립토 프로젝트 미션 관리, 할일 목록, 중요 일정 관리

---

## 📁 디렉토리 구조

```
infofi2/
├── .git/                          # Git 버전 관리
├── .gitignore                     # Git 제외 파일 목록
├── components/                    # React 컴포넌트 디렉토리
│   ├── icons/                     # 아이콘 컴포넌트
│   │   ├── EditIcon.tsx
│   │   ├── GrabHandleIcon.tsx
│   │   ├── LinkIcon.tsx
│   │   ├── PlusIcon.tsx
│   │   ├── TrashIcon.tsx
│   │   └── UserPlusIcon.tsx
│   ├── ConfirmationModal.tsx      # 확인 모달
│   ├── Header.tsx                 # 헤더 컴포넌트
│   ├── ImageDropzone.tsx          # 이미지 드롭존
│   ├── ImportantDateModal.tsx     # 중요 일정 추가 모달
│   ├── ImportantDates.tsx         # 중요 일정 목록
│   ├── Notes.tsx                  # 노트 컴포넌트
│   ├── PinnedMissions.tsx         # 고정된 미션 (야핑 리더보드)
│   ├── QuickLinkModal.tsx         # 퀵링크 추가 모달
│   ├── QuickLinks.tsx             # 퀵링크 목록
│   ├── Sidebar.tsx                # 사이드바 (드래그 앤 드롭 지원)
│   ├── SidebarPanel.tsx           # 사이드바 패널 래퍼
│   ├── TaskItem.tsx               # 개별 태스크 아이템
│   ├── TaskList.tsx               # 태스크 목록
│   ├── TaskModal.tsx              # 태스크 추가/수정 모달
│   ├── TodoItem.tsx               # 개별 할일 아이템
│   └── TodoList.tsx               # 할일 목록
├── firebase/                      # Firebase 설정
│   └── firebase.ts
├── hooks/                         # 커스텀 훅
│   └── useLocalStorage.ts         # 로컬스토리지 훅
├── App.tsx                        # 메인 앱 컴포넌트
├── index.html                     # HTML 엔트리 포인트
├── index.tsx                      # React 엔트리 포인트
├── types.ts                       # TypeScript 타입 정의
├── vite.config.ts                 # Vite 설정
├── tsconfig.json                  # TypeScript 설정
├── package.json                   # 프로젝트 의존성
├── metadata.json                  # 프로젝트 메타데이터
└── README.md                      # 프로젝트 설명서
```

---

## 🎯 핵심 기능

### 1. **미션 관리 (Mission Management)**
- 크립토 프로젝트 미션 추가/수정/삭제
- 미션 완료 상태 토글
- 미션별 로고, URL, 설명, 태그, 종료일, 레퍼럴 URL 관리
- 자동 파비콘 가져오기 (icon.horse API 사용)

### 2. **사이드바 위젯 시스템**
- **드래그 앤 드롭**: `react-beautiful-dnd`를 사용한 위젯 재배치
- **2열 레이아웃**: 사용자 정의 가능한 레이아웃
- **위젯 종류**:
  - 야핑 리더보드 (Yaping 태그 필터링)
  - 퀵링크
  - 중요 일정
  - 할일 목록
  - 노트

### 3. **데이터 관리**
- **로컬스토리지**: 모든 데이터를 브라우저에 저장
- **데이터 백업/복원**: JSON 파일로 내보내기/가져오기
- **Firebase 준비**: firebase 디렉토리 존재 (향후 클라우드 동기화 가능)

---

## 🔧 기술 스택 상세

### **프론트엔드**
- **React 18.2.0**: 함수형 컴포넌트 + Hooks
- **TypeScript**: 타입 안전성
- **Vite**: 빠른 개발 서버 및 빌드 도구
- **TailwindCSS**: 유틸리티 우선 CSS (CDN 방식)

### **주요 라이브러리**
- **react-beautiful-dnd**: 드래그 앤 드롭 기능
- **ESM.sh**: 브라우저 ESM 모듈 로딩

### **개발 도구**
- **TypeScript 5.8.2**
- **Vite 6.2.0**
- **@vitejs/plugin-react**: React Fast Refresh

---

## 📦 데이터 모델 (types.ts)

### **Task (미션)**
```typescript
interface Task {
  id: string;
  title: string;              // 미션 제목
  url: string;                // 미션 URL
  description: string;        // 설명
  tags: string[];             // 태그 (예: Daily, Airdrop, Yaping)
  isCompleted: boolean;       // 완료 여부
  logo?: string;              // 로고 이미지 (Base64)
  endDate?: string;           // 종료일
  referralUrl?: string;       // 레퍼럴 URL
}
```

### **Todo (할일)**
```typescript
interface Todo {
  id: string;
  text: string;
  isCompleted: boolean;
}
```

### **QuickLink (퀵링크)**
```typescript
interface QuickLink {
  id: string;
  title: string;
  url: string;
}
```

### **ImportantDate (중요 일정)**
```typescript
interface ImportantDate {
  id: string;
  date: string;
  text: string;
}
```

### **SidebarLayout (사이드바 레이아웃)**
```typescript
interface SidebarLayout {
  column1: string[];  // 첫 번째 열의 위젯 순서
  column2: string[];  // 두 번째 열의 위젯 순서
}
```

---

## 🎨 UI/UX 특징

### **디자인 시스템**
- **배경**: 그라디언트 (rose-100 → teal-100)
- **글래스모피즘**: 반투명 배경 + 백드롭 블러
- **색상 팔레트**:
  - Primary: `#fdf2f8` (연한 파스텔)
  - Accent: `#d946ef` (푸시아)
  - Light: `#1f2937` (다크 그레이 - 메인 텍스트)
  - Medium: `#6b7280` (라이트 그레이 - 보조 텍스트)

### **반응형 디자인**
- 모바일: 1열 레이아웃
- 데스크톱: 2열 레이아웃 (미션 대시보드 + 사이드바)

### **인터랙션**
- 호버 효과
- 부드러운 트랜지션
- 드래그 앤 드롭 시각적 피드백
- 플로팅 액션 버튼 (우측 하단)

---

## 🔄 상태 관리

### **App.tsx의 상태**
```typescript
// 로컬스토리지 기반 상태
const [tasks, setTasks] = useLocalStorage<Task[]>('crypto-missions:tasks', []);
const [todos, setTodos] = useLocalStorage<Todo[]>('crypto-missions:todos', []);
const [notes, setNotes] = useLocalStorage<string>('crypto-missions:notes', '');
const [quickLinks, setQuickLinks] = useLocalStorage<QuickLink[]>('crypto-missions:quickLinks', []);
const [importantDates, setImportantDates] = useLocalStorage<ImportantDate[]>('crypto-missions:importantDates', []);
const [sidebarLayout, setSidebarLayout] = useLocalStorage<SidebarLayout>('crypto-missions:sidebarLayout', defaultSidebarLayout);

// 모달 상태
const [isModalOpen, setIsModalOpen] = useState(false);
const [taskToEdit, setTaskToEdit] = useState<Task | null>(null);
const [taskToDelete, setTaskToDelete] = useState<string | null>(null);
```

### **useLocalStorage 훅**
- 로컬스토리지와 React 상태를 동기화
- 초기값 설정 지원
- 자동 JSON 직렬화/역직렬화

---

## 🚀 주요 컴포넌트 분석

### **1. App.tsx (메인 컴포넌트)**
- **역할**: 전체 앱의 상태 관리 및 레이아웃
- **주요 기능**:
  - 미션 CRUD 작업
  - 데이터 백업/복원
  - 모달 관리
  - 진행률 표시

### **2. Sidebar.tsx (사이드바)**
- **역할**: 드래그 가능한 위젯 컨테이너
- **주요 기능**:
  - `react-beautiful-dnd`를 사용한 드래그 앤 드롭
  - 2열 레이아웃 관리
  - 위젯 필터링 (야핑 미션 자동 필터)
  - 모달 상태 관리

### **3. TaskModal.tsx (미션 모달)**
- **역할**: 미션 추가/수정 폼
- **주요 기능**:
  - 폼 입력 관리
  - 자동 파비콘 가져오기 (URL 입력 시)
  - 이미지 드롭존 통합
  - 유효성 검사

### **4. TaskItem.tsx (미션 아이템)**
- **역할**: 개별 미션 표시
- **주요 기능**:
  - 완료 상태 토글
  - 태그 표시
  - 종료일 표시
  - 수정/삭제 버튼

### **5. ImageDropzone.tsx (이미지 업로더)**
- **역할**: 드래그 앤 드롭 이미지 업로드
- **주요 기능**:
  - 파일 드래그 앤 드롭
  - 이미지 미리보기
  - Base64 인코딩

---

## 🔌 API 및 외부 서비스

### **1. icon.horse API**
- **용도**: 웹사이트 파비콘 가져오기
- **엔드포인트**: `https://icon.horse/icon/{hostname}`
- **사용 위치**: TaskModal.tsx의 `handleUrlBlur` 함수

### **2. Gemini API (준비 중)**
- **설정 파일**: vite.config.ts
- **환경 변수**: `GEMINI_API_KEY`
- **상태**: 설정은 되어 있으나 현재 미사용

---

## 📊 데이터 흐름

```
사용자 입력
    ↓
App.tsx (상태 관리)
    ↓
useLocalStorage 훅
    ↓
localStorage (브라우저)
    ↓
컴포넌트 렌더링
```

### **백업/복원 흐름**
```
내보내기: 상태 → JSON → Blob → 다운로드
가져오기: 파일 선택 → FileReader → JSON 파싱 → 상태 업데이트
```

---

## 🎯 특별한 기능

### **1. 야핑 리더보드 자동 필터링**
```typescript
const yapingMissions = useMemo(() => tasks.filter(task => 
  task.tags.some(tag => tag.toLowerCase() === 'yaping')
), [tasks]);
```
- "Yaping" 태그가 있는 미션만 자동으로 필터링
- 야핑 미션이 없으면 위젯 숨김

### **2. 진행률 표시**
```typescript
const completedTasks = tasks.filter(t => t.isCompleted).length;
const totalTasks = tasks.length;
// 진행률 바: (completedTasks / totalTasks) * 100%
```

### **3. 레이아웃 커스터마이징**
- 사용자가 위젯을 드래그하여 원하는 위치로 이동
- 레이아웃 설정이 로컬스토리지에 저장됨

---

## 🛠️ 개발 환경 설정

### **필수 요구사항**
- Node.js

### **실행 방법**
```bash
# 의존성 설치
npm install

# 개발 서버 실행 (포트 3000)
npm run dev

# 프로덕션 빌드
npm run build

# 빌드 미리보기
npm run preview
```

### **환경 변수**
- `.env.local` 파일에 `GEMINI_API_KEY` 설정 (선택사항)

---

## 🔐 보안 고려사항

1. **로컬 데이터 저장**: 모든 데이터가 브라우저 로컬스토리지에 저장됨
2. **외부 API 호출**: icon.horse API만 사용 (HTTPS)
3. **XSS 방지**: React의 기본 XSS 보호 활용
4. **이미지 업로드**: Base64로 인코딩하여 로컬 저장

---

## 🚧 향후 개선 가능 사항

1. **Firebase 통합**: 클라우드 동기화 및 멀티 디바이스 지원
2. **검색 기능**: 미션 검색 및 필터링
3. **정렬 기능**: 날짜, 태그, 완료 상태별 정렬
4. **알림 기능**: 중요 일정 알림
5. **다크 모드**: 테마 전환 기능
6. **미션 통계**: 완료율, 태그별 통계 대시보드
7. **Gemini API 활용**: AI 기반 미션 추천 또는 자동 분류

---

## 📝 코드 품질

### **장점**
- ✅ TypeScript로 타입 안전성 확보
- ✅ 함수형 컴포넌트 + Hooks 사용
- ✅ useCallback으로 불필요한 리렌더링 방지
- ✅ useMemo로 계산 최적화
- ✅ 컴포넌트 분리가 잘 되어 있음
- ✅ 로컬스토리지 커스텀 훅으로 재사용성 증가

### **개선 가능 영역**
- ⚠️ 에러 핸들링 강화 필요
- ⚠️ 로딩 상태 표시 추가
- ⚠️ 접근성(a11y) 개선 여지
- ⚠️ 단위 테스트 부재

---

## 🎓 학습 포인트

이 프로젝트를 통해 배울 수 있는 것들:

1. **React 상태 관리**: useState, useCallback, useMemo, 커스텀 훅
2. **로컬스토리지 활용**: 브라우저 저장소 API
3. **드래그 앤 드롭**: react-beautiful-dnd 라이브러리
4. **모달 패턴**: 모달 상태 관리 및 이벤트 처리
5. **파일 처리**: FileReader API, Blob, Base64
6. **외부 API 통합**: Fetch API, 에러 핸들링
7. **TypeScript**: 인터페이스, 제네릭, 타입 추론
8. **Vite**: 모던 빌드 도구 설정

---

## 📞 프로젝트 링크

- **AI Studio**: https://ai.studio/apps/drive/1Y8a4uBO43v3sXbyQ9wwN6scWWNO3nBb7
- **로컬 개발 서버**: http://localhost:3000
- **GitHub**: https://github.com/lovedanischoice-del/infofi2

---

## 🚀 배포 정보

### **Cloudflare Pages 배포**
- **상태**: 배포 준비 완료 ✅
- **빌드 명령어**: `npm run build`
- **출력 디렉토리**: `dist`
- **Node.js 버전**: 18+

### **해결된 배포 문제** (2026-02-11)
1. ✅ Import Map 제거 - ESM.sh 충돌 해결
2. ✅ React 18로 다운그레이드 - react-beautiful-dnd 호환성
3. ✅ Vite 설정 최적화 - base path 및 빌드 옵션 추가

### **배포 가이드**
- 상세 가이드: `CLOUDFLARE_DEPLOY.md`
- 배포 방법: `배포_방법.md`
- 문제 해결 기록: `배포_문제_해결_완료.md`

---

**작성일**: 2026-02-11  
**분석 버전**: 1.1  
**마지막 업데이트**: 2026-02-11 (배포 설정 추가)
